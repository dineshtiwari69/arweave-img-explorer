/** @type {import('next').NextConfig} */
/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['permagate.io'],
      dangerouslyAllowSVG: true,
    },
  }
export default nextConfig;
